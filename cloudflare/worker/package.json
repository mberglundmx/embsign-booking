{
  "name": "brf-booking-cloudflare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply brf-booking-d1 --local",
    "d1:migrate:remote": "wrangler d1 migrations apply brf-booking-d1 --remote"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
